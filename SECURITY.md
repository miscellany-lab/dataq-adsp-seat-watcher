# Security Policy

Do not commit Telegram bot tokens, chat IDs, OCR debug dumps, browser profiles, or logs.

If a Telegram bot token is exposed, revoke it immediately through `@BotFather` and issue a new token.

This project does not accept or provide code for automatic login, automatic registration, automatic payment, CAPTCHA bypass, or site automation-detection bypass.
